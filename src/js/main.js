import { Slider } from './modules/functions.js';

const projectSliderElement = document.querySelector('.projects-slider__slider');
const projectSlider = new Slider(projectSliderElement, 'projects-slider')
